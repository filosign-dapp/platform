const INDEXER = {
  CONFIRMATIONS: 6n,
  MAX_BATCH_BLOCKS: 200n,
  POLL_INTERVAL_MS: 3_000,
  LOGS_PER_TX: 10,
  DEFAULT_START_BLOCK: 3_044_275n,
};

const config = {
  INDEXER,
};

export default config;
